import { AppwriteClient } from "./index";

describe("appwrite-client-typescript-sdk", () => {
    it("initialize client", async () => {
        const appwriteclient = new AppwriteClient({
        });
    });
});
